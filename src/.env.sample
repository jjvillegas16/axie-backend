# This should be the service name of mongo in docker-compose.yml
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=

# bcrypt
BCRYPT_ROUNDS=

# jwt
# can be generated using: 
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET_KEY=